{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %} {# Сделать тут слаг #}

{% block body %}
    <h1>{{ product.name }}</h1>

    <div class="content">
        {{ include('Components/ShowComponents/product.html.twig') }}

        <p id="new-record">
            <a href="{{ path('app_product_setformfororder', {name: product.name}) }}">Buy</a>
        </p>
    </div>
{% endblock %}

